# Семантика

## 1. Почему семантическая верстка важна?
Когда мы создаем веб-страницы, мы не просто комбинируем различные элементы визуального дизайна. Вместо этого, мы должны представить контент логически и иерархически. Использование семантических тегов позволяет нам выразить смысл каждого элемента, что улучшает понимание содержания страницы как разработчиками, так поисковыми системами и скринридерами.
## Пример семантической верстки
### Без семантики:
```
<div className="title">Title</div>
<div className="text">Huge text</div>

<div className="list">
  <div className="some-sheet-bool-sheet-element">
    Item 
  </div>
</div>

<div className="date">2001-05-15</div>
<div className="status">Status</div>
```
### С семантической версткой:
```
<h1 className="title">Title</h1>
<p className="text">Huge text</p>

<ul className="list">
  <li className="some-sheet-bool-sheet-element">
    Item
  </li>
</ul>

<time className="date">2001-05-15</time>
<strong className="status">Status</strong>
```
## 2. Структура страницы

### `<header>` - Заголовочная часть страницы. Только 1 на странице 

### `<main>` - Основное содержимое страницы.  Только 1 на странице

### `<footer>` - Нижняя часть страницы. Только 1 на странице 

### `<section>` - Секция контента. 

### `<article>` - Самодостаточный раздел документа.

### `<aside>` - Sidebar 

### `<nav>` - Элементы навигации по сайту. Указывать nav только самые важные [Дока](https://doka.guide/html/nav/#podskazki) , [Спецификация](https://html.spec.whatwg.org/multipage/sections.html#the-nav-element) 

### 
```
<header> 
  <nav>
    <ul>
      <li></li>
    </ul>
  </nav>
</header>
<aside></aside>
<main>
  <section></section>
  <article></article>
</main>
<footer></footer>
```
## 3. Главные семантические теги

### 1. `<h1>...<h6>`
 #### Кратко
 Заголовки на странице для оформления структуры.
 #### Пример
 ```
 <h1>Самый важный заголовок</h1>
 <h2>Заголовок уровня 2</h2>
 <h3>Заголовок уровня 3</h3>
 <h4>Заголовок уровня 4</h4>
 <h5>Заголовок уровня 5</h5>
 <h6>Заголовок уровня 6</h6>
```
#### 💡 Hints:
 
💡 На странице должен быть только один основной заголовок `<h1>` . Это помогает поисковым системам понять основную тему страницы

💡 После тега `<h1>` использовать только `<h2>` , а не `<h3>` 

💡 Использовать уровни заголовков на основе их иерархической значимости, а не только для визуального вида. Для изменения размера шрифта использовать "font-size", а не менять 
уровень заголовка
 
---
### 2. `<p>...</p>`
 #### Кратко
 Обозначение абзацев (параграфов)
 #### Пример
 ```
 <p>Это первый абзац текста.</p>
 <p>Это второй абзац текста.</p>
 ```
#### 💡 Hints: 

💡 Тег `<p>` по факту ведёт себя как обычный блок `<div>`

💡 Оборачивать минимум одно предложение. Если слово/словосочетание - `<span>`.

---
### 3. `<ul> <ol> <li> списки `

#### Кратко
Тег `<ul>` создание неупорядочных маркированных списков, (точки, кружки или квадраты).

Тег `<ol>` создание упорядочных маркированных списков, (цифрами, буквами).

Тег `<li>` определяет элемент списка. Должен быть вложен в тег `<ul>` или `<ol>`.
#### Пример
```
<ul>
  <li>Первый элемент</li>
  <li>
    <ol>
      <li>Первый элемент</li>
      <li>Второй элемент</li>
    </ol>
  </li>
  <li>Третий элемент</li>
</ul>
```
<ul>
  <li>Первый элемент</li>
  <li>
    <ol>
      <li>Первый элемент</li>
      <li>Второй элемент</li>
    </ol>
  </li>
  <li>Третий элемент</li>
</ul>

#### 💡 Hints: 

💡 Дочерними тегами `ul` `ol` обязательно должны быть `<li>`, в которых уже вложены любые другие теги.

💡 `<ol>`: есть явный/неявный порядок элементов, например: план дел на день, `<ul>`: порядок не имеет значения.

---
### 4. `<a>...</a>`

#### Кратко

Тег `<a>` используется для создания гиперссылок, позволяющих переходить на другие страницы.

#### Пример

```
<a href="https://www.example.com">Ссылка на пример</a>
```

#### 💡 Hints:

💡 Чтобы отправить пользователя в самый верх страницы: href="#top" или href="#".

💡 Если якорная ссылка пролистывает слишком много/мало:
```
h2 {
  scroll-margin: 20px;
}
```

💡 хотим disabled ссылку: [https://doka.guide/html/a/#olga-veter-sovetuet](https://doka.guide/html/a/#olga-veter-sovetuet)

💡 в любой ситуации если выбираем между кнопкой и ссылкой выбираем `<button>`

💡 Если можем дать адрес для перенаправления это ссылка (`<a href="">`).

💡 Если открываем диалоговое окно или листаем слайдер, то это  `<button>`.

💡
##### Так не нужно делать!
```
<a href="#">
  <h2>Заголовок, он же ссылка</h2>
</a>
```
##### Хорошо
```
<h2>
  <a href="#">Заголовок, он же ссылка</a>
</h2>
```

---
### 5. `<button>...</button>`

#### Кратко

Тег `<button>` чтобы добавить на страницу кнопку.

#### Пример

```
<form id="login">
	<label>
		Введите ваш email
		<input type="email" placeholder="Ваш email" />
	</label>
	<button name="button">Нажми меня</button>
</form>
<button type="submit" form="login">Отправить</button>
```

#### 💡 Hints:

💡 Если `<button>` должен отправлять данные, то `<button>` должен быть внутри контейнера `<form>`, либо связан с формой при помощи атрибутов `id` и `form`

💡 в любой ситуации если выбираем между кнопкой и ссылкой выбираем `<button>`

💡 Если можем дать адрес для перенаправления это ссылка (`<a href="">`).

💡 Если открываем диалоговое окно или листаем слайдер, то это  `<button>`.
### 6. `<img>`

#### Кратко

Тег `<img>` используется для вставки изображений на веб-страницу.

#### Пример

```
<img src="example.jpg" alt="Пример изображения">
```

#### 💡 Hints:

💡 для изменения использовать `object-fit` , `object-position`  и тд `object`

💡 [указывать `width` `height` обязательно](https://habr.com/ru/articles/524918/)

---
### 7. `<video>`

#### Кратко

Тег `<video>` используется для вставки видео на страницу.

#### Пример

```
<video controls poster="flower.png" width="250">
  <source src="flower.webm" type="video/webm">
  <source src="flower.mp4" type="video/mp4">
  Ваш браузер не поддерживает встроенные видео :( 
</video>
```

#### 💡 Hints:

💡 для изменения использовать `object-fit` , `object-position`  и тд `object`

💡 [указывать `width` `height` обязательно](https://habr.com/ru/articles/524918/)

💡 чтобы работал `autoplay` атрибут нужен `muted` 

💡 Если не указать атрибут `controls`, то браузер не будет показывать стандартные элементы управления видеоплеером.

---
## 4. Формы
### 1. `<form>`

#### Кратко

Тег  `<form>` нативный элемент для формы

#### Пример

```
<form action="" method="post">
  <fieldset>
    <legend>Выберите прожарку</legend>
    <label> <input type="radio" name="level" /> Rare </label>
    <label> <input type="radio" name="level" checked /> Medium </label>
    <label> <input type="radio" name="level" /> Well Done </label>
  </fieldset>
</form>
```

#### [Обработка формы](https://doka.guide/js/deal-with-forms/)
#### 💡 Hints:

💡 Использовать `method` и `action` для обработки и отправки данных 

💡 Никогда не использовать `method="get"`, если отправлять конфиденциальные данные, их можно будет легко прочитать в запросе и даже в адресной строке браузера.

💡По умолчанию отправка формы через `method` и `action` происходит синхронно — отправляет запрос и рисует на экран все, что вернётся в ответ. Что приводит к полной перезагрузке страницы.

---
### 2. `<fieldset>`  `<legend>` контейнеры в форме

#### Кратко

Тег `<fieldset>` контейнер внутри формы для одинаковых по назначению элементов.

Тег  `<legend>` добавляет заголовок в `<fieldset>`

#### Пример

```
<form>
  <fieldset>
    <legend>Личная информация</legend>
    <div>
      <label for="name">Имя:</label> 
      <input id="name" type="text" name="name" />
    </div>
    <div>
      <label for="email">Email:</label> 
      <input id="email" type="email" name="email" />
    </div>
  </fieldset>
  <fieldset>
    <legend>Настройки аккаунта</legend>
    <div>
      <label for="username">Имя пользователя:</label>
      <input id="username" type="text" name="username" />
    </div>
    <div>
      <label for="password">Пароль:</label>
      <input id="password" type="password" name="password" />
    </div>
  </fieldset>
  <button type="submit">Отправить</button>
</form>
```

#### 💡 Hints:

💡 Есть возможность заблокировать все теги внутри наложив `disabled`

💡 Рамку можно стилизовать


---
### 3. `<input>` 

#### Кратко

Тег  `<input>`  используется для создания интерактивных элементов.

#### Пример

```
<label for="search" > Текстовое поле в одну строчку: 
  <input id="search" type="text" size="40"> 
</label>
```

#### 💡 Hints:

💡 нужно стилизовать hover, focus, focus-within

💡 использовать в паре с label для скринридеров 

💡 то как работает `<input>` зависит от type

💡 если `type="number"` то `type="tel"`

💡вместо использования `type="button | reset | submit"` - использовать `<button>`

---
### 4. `<label>` 

#### Кратко

Тег  `<label>`  подписывает поля ввода и другие элементы формы.

#### Пример

```
<label for="search" > Текстовое поле в одну строчку: 
  <input id="search" type="text" size="40"> 
</label>
```

#### 💡 Hints:

💡 `<label>` нужен для ссылок на связанный элемент, так как нажатие на `<label>` вызывает  событие клика также и на связанном элементе.

💡 Обязательно использовать для инпутов, чекбоксов, радиокнопок

💡 Обязательно даже если нету `<label>` то сделать скрытый для скринридеров

---
## 5. Остальные теги: 

### 1. `<time>` - Дата/время.

### 2. `<strong>` - Жирный текст.

### 3. `<blockquote>` - Цитата.

### 4. `<cite>` - Названия произведения, источников цитаты.

### 5. `<code>` - Код. Вставлять внутрь `<pre>`.
<pre><code> &lt;div&gt;Какой-то текст&lt;/div&gt; </code></pre>

### 6. `<pre>` - Текст без обработки сохраняя пробелы и переносы (хорошо подходит к `<code>`).

### 7. `<abbr>` - Сокращение или аббревиатура.

### 8. `<address>` - Контактная информация.

### 9. `<figure>` - `<img>` с подписью.
### `<figcaption>` - Подпись к `<figure>`.

### 10. `<details>` - Нативный «аккордеон» на HTML.

### 11. `<summary>` -  заголовок «аккордеона».

### 12. `<select>` - Нативный раскрывающийся список.
### `<option>` - пункт в раскрывающемся списке `<select>`.

### 13. `<dialog>` - Диалоговое окно.
